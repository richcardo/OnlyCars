import styled from "@emotion/styled"

export const StyledLinksList = styled.ul`
  list-style: none;
  display: flex;
  flex-flow: column wrap;
  gap: 2.5rem;
  padding: 0;
  margin: 0;
  & button {
    padding: 0;
    font-weight: lighter;
    font-size: large;
    aspect-ratio: 0;
  }
`
